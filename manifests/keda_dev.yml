apiVersion: keda.sh/v1alpha1
kind: ScaledObject
metadata:
  name: demo-ui-scaler
  namespace: development
spec:
  scaleTargetRef:
    name: demo-ui-deployment
  minReplicaCount: 2
  maxReplicaCount: 10
  triggers:
  - type: cpu
    metadata:
      type: Utilization
      value: "70"
